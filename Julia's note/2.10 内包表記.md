### 1. 内包表記
- set builder notationで書ける集合をコード化
- 得られる結果はベクトル
- 数式と同様の処理が可能
例：
$\mathcal{S} = \{ x^2 \mid x \in \{ 1, \dots, 5 \} \}$
```
S = [x^2 for x in 1:5]
```
- 多次元に拡張も可能
例：
```
[2i + j for i in 1:4, j in 1:3]
```

### 2. if文との併用
- `if`での振り分け
```
[x for x in x_array if x < n ]
```