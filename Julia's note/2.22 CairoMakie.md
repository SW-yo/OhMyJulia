### 1. CairoMakie
-　基本的な使い方
```julia
fig = Figure(size=(400, 400))    # size指定は無くても良い 
axis = Axis(fig[1,1])
lines!(axis, 0..5, sin)
fig
```
- `Point2f` : `Vector{Vector{Float64}}`を2軸のデータに変換する。
```julia
# x_new:: Vector{Vector{Float64}}
scatter!(ax, Point2f.(x_new))
```