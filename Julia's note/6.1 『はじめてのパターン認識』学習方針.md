### 1. 方針
✔線形識別関数のロジスティック回帰をスクラッチで
✔多層パーセプトロンのバックプロパゲーションを手書き
✔カーネル主成分分析で固有値分解して非線形カーネルを使ったサポートベクターマシン実装
✔ベイズ混合ガウスモデルでEMアルゴリズムを実装

### 2. 線形識別関数のロジスティック回帰をスクラッチで
1. アヤメデータ（iris.csv）をGitHub（https://vincentarelbundock.github.io/Rdatasets/datasets.html / item：iris）から落としてくる。
2. データセットが実行例4.1（p.39）と同じか確かめるため、花弁の長さ（Sepal.Length）と花弁の幅（Sepal.Width）で品種（Species）ごとにマークを変えてプロット。⇒iris_01-simple_plot.jl
3. Boolに変換する