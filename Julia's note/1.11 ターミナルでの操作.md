### 1. ディレクトリ操作
- カレントディレクトリ：`pwd()`
- カレント下のディレクトリへ移動：`cd("[directory]")`
- 上の階層への移動：`cd("..")
- 上がってから別のディレクトリへ移動：cd("../\[directory]")`
- `;`でshellモードに移ってからの移動：cd ..　※Juliaモードに戻るのは`back`

### 2. ファイルの実行
- コマンドでの実行：`include("[filename]")`
- 保存してからでないと内容が反映されない。
- マウス操作での実行は▷